import { ChannelList } from './channel-list';

export const Sidebar = () => {
  return (
    <div className="flex h-full w-60 flex-col bg-gray-800">
      {/* Sidebar content */}
    </div>
  );
};

export default Sidebar; 