model Channel {
  id          String    @id @default(cuid())
  name        String
  description String?
  type        String    // PUBLIC, PRIVATE, or DM
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  createdById String
  members     Member[]
  messages    Message[]

  @@index([createdById])
}

model Member {
  id        String   @id @default(cuid())
  userId    String
  channelId String
  role      String   // ADMIN or MEMBER
  joinedAt  DateTime @default(now())
  channel   Channel  @relation(fields: [channelId], references: [id], onDelete: Cascade)

  @@unique([userId, channelId])
  @@index([channelId])
  @@index([userId])
}

// Add other models as needed (Message, etc.) 